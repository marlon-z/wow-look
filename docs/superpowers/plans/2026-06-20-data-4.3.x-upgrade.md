# 4.3.x Equipment Data Upgrade Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace the project raw equipment export with the verified 3.3.3 SavedVariables file and generate `cos-upload/data-4.3.x`.

**Architecture:** Keep the raw game export as the source of truth, merge the existing tier export during conversion, and write a new versioned output directory. Extend the converter's new-export path to honor `--data-version` without changing default behavior.

**Tech Stack:** Node.js, Lua SavedVariables, JSON, CommonJS data modules, PowerShell verification.

---

## Chunk 1: Version-aware conversion

### Task 1: Make new-export conversion honor data version

**Files:**
- Modify: `scripts/parse-export.js`
- Create: `scripts/test-data-version-output.js`

- [x] Add a failing static/unit check that `--data-version` reaches class and overview builders in new-export mode.
- [x] Add an optional `dataVersion` argument to `buildClassPayload` and `buildOverviewPayload`.
- [x] Write `version` and `dataVersion` as `4.3.x` when the option is supplied; preserve the game Build default otherwise.
- [x] Run Node syntax and data-version tests.

## Chunk 2: Raw input and generated artifacts

### Task 2: Replace and validate the raw export

**Files:**
- Replace: `WoWLookExport3.lua`

- [x] Copy the verified client SavedVariables file over the project root input.
- [x] Compare SHA256 hashes.
- [x] Run `node scripts/test-max-item-level-export.js --input WoWLookExport3.lua`.

### Task 3: Generate the 4.3.x dataset

**Files:**
- Create: `cos-upload/data-4.3.x/*.json`
- Create: `cos-upload/data-4.3.x/*.js`

- [x] Generate a temporary dataset using `WoWLookExport3.lua`, `WoWLookTierExport.lua`, `--data-version 4.3.x`, and shared assets.
- [x] Validate 13 class pairs plus overview, JSON parsing, CommonJS loading, tier instance presence, and version metadata.
- [x] Generate/promote the verified dataset to `cos-upload/data-4.3.x`.
- [x] Verify item `258514` is 298 and carries Voidforged weapon bonuses.
- [x] Confirm `cos-upload/data-4.2.x` and web/miniprogram version configuration remain unchanged.
- [x] Run `git diff --check` and review status.
